[tool.isort]
profile = "black"

[tool.black]
line-length = 120

[tool.mypy]
exclude = [
    "^pyfred/tests",
    "^pyfred/template",
]

[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools ~= 65.5", "wheel ~= 0.37.1"]

[tool.pytest.ini_options]
pythonpath = [
  "."
]
